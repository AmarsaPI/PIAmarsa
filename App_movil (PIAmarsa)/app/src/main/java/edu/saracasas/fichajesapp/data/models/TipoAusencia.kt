package edu.saracasas.fichajesapp.data.models

enum class TipoAusencia {
    /** Vacaciones del empleado. */
    VACACIONES,

    /** Baja médica. */
    BAJA_MEDICA,

    /** Permiso retribuido. */
    PERMISO_RETRIBUIDO
}